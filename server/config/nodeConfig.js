import config from '../modules/config.js';

export const nodeConfig = config({

    /* ==================================== */
    /* =    Режим загрузки приложения     = */
    /* ==================================== */
    NODE_ENV: 'development',

});
