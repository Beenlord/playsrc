import config from '../utils/config.js';

export const appConfig = config({

    /* ==================================== */
    /* =          Хост приложения         = */
    /* ==================================== */
    APP_HOST: 'localhost',

    /* ==================================== */
    /* =          Порт приложения         = */
    /* ==================================== */
    APP_PORT: 3000,

});
