import config from '../utils/config.js';

export const redisConfig = config({

	/* ==================================== */
	/* =       Хост redis сервера         = */
	/* ==================================== */
	REDIS_HOST: '127.0.0.1',

	/* ==================================== */
	/* =       Порт redis сервера         = */
	/* ==================================== */
	REDIS_PORT: 6379,

});
